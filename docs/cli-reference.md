# CLI Reference

- `parasol init` — create `~/.parasol/` and seed config key
- `parasol encrypt <file>` — write `<file>.parasol`
- `parasol decrypt <file>.parasol` — decrypt to original file name
- `parasol audit` — print local audit table
- `parasol rotate-keys` — rotate key and re-encrypt local `.parasol` files
